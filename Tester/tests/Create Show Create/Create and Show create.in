@test "def2"
{
@tail:="I FLOAT PRIMARY KEY"
@for(@i:= 0;@i<100;@i:=@i+1)
{
    @tail := @tail + ",I"+@i+" INT, NAME"+@i+" CHAR(30) UNIQUE, U"+@i+" FLOAT"
    @test : "CREATE TABLE T ("+@tail+")"
    @test : "SHOW CREATE TABLE T"
    @sql : "DROP TABLE T"
}
}